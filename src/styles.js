const white = "#ffffff";
const dark = "#DFE4EA";
const shadowTl = "-4px -2px 4px 0px";
const shadowBr = "4px 2px 6px 0px";
const radius = "3px";
const gray = "hsla(210, 38%, 95%, 1)";
const muted = "hsla(214, 25%, 65%, 1)";
const black = "#4a5568";

export const theme = {
  backgroundColor: "#f5f6f7",
  pink: "hotpink",
  shadow: `${shadowTl} ${white}, ${shadowBr} ${dark}`,
  shadowFlip: `${shadowTl} ${dark} inset, ${shadowBr} ${white} inset`,
  radius,
  white,
  dark,
  gray,
  muted,
  black,
}

